# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.agents
open-design-temp
skills
.clinerules

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.env*

# Playwright E2E
# `e2e/.auth/user.json` contains a real Supabase + Google session
# (access_token, refresh_token, provider_token) — must never be
# committed. The .gitkeep placeholder keeps the directory tracked.
e2e/.auth/user.json
e2e/.auth/*-journal
playwright-report/
test-results/
